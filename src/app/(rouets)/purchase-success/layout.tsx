interface SuccessPageLayoutProps {
  children: React.ReactNode;
}

const SuccessPageLayout = async ({ children }: SuccessPageLayoutProps) => {
  return <>{children}</>;
};

export default SuccessPageLayout;
